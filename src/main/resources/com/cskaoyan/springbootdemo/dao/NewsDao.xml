<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cskaoyan.springbootdemo.dao.NewsDao">
   <!-- <select id="insert"   parameterType="news" resultType="Integer">
        insert into news (id, title,image,likecount,commentcount,createddate,userid)
        values (null,#{title},#{image},#{likeCount},#{commentCount},#{createdDate},#{userId})
    </select>-->

</mapper>